# homebrew-tap — agent orientation

## Identity (workspace architecture)

**Scope:** Homebrew formulas for Croft local tooling. Follow the global Homebrew workflow rules (`~/.claude/CLAUDE.md`): brew install from the tap, push before install, verify SHA256 against release assets.
**Not this repo:** the tools themselves (their own repos).
Card + altitudes: `CroftC/.claude/ARCHITECTURE.md`. Concurrent-session norms:
`CroftC/.claude/COORDINATION.md` (multi-turn work in `worktrees/homebrew-tap/<slug>`,
never this checkout).
